---
import Container from '@/components/common/Container.astro';
import Card from '@/components/common/Card.astro';
import Button from '@/components/common/Button.astro';
import { SITE_CONFIG } from '@/config/site.config';
---

<section class="py-20 bg-[#0A0A0A]">
  <Container>
    <div class="max-w-2xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-6xl font-black text-white mb-4">
          Trabajemos <span class="text-[#D4FF00]">Juntos</span>
        </h2>
        <p class="text-xl text-gray-400">
          ¿Tienes un proyecto en mente? Me encantaría escuchar sobre él
        </p>
      </div>
      
      <Card padding="lg" className="border-2 border-[#D4FF00]/20">
        <form class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-bold text-white mb-2 uppercase">
              Nombre
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 bg-black/50 border-2 border-gray-800 text-white rounded-lg focus:ring-2 focus:ring-[#D4FF00] focus:border-[#D4FF00] transition"
              placeholder="Tu nombre"
            />
          </div>
          
          <div>
            <label for="email" class="block text-sm font-bold text-white mb-2 uppercase">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 bg-black/50 border-2 border-gray-800 text-white rounded-lg focus:ring-2 focus:ring-[#D4FF00] focus:border-[#D4FF00] transition"
              placeholder="tu@email.com"
            />
          </div>
          
          <div>
            <label for="message" class="block text-sm font-bold text-white mb-2 uppercase">
              Mensaje
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              required
              class="w-full px-4 py-3 bg-black/50 border-2 border-gray-800 text-white rounded-lg focus:ring-2 focus:ring-[#D4FF00] focus:border-[#D4FF00] resize-none transition"
              placeholder="Cuéntame sobre tu proyecto..."
            ></textarea>
          </div>
          
          <Button type="submit" size="lg" className="w-full">
            Enviar Mensaje
          </Button>
        </form>
      </Card>
      
      <div class="mt-12 text-center">
        <p class="text-gray-400 mb-4 uppercase font-bold tracking-wide">O contáctame directamente:</p>
        <a 
          href={`mailto:${SITE_CONFIG.email}`}
          class="text-[#D4FF00] hover:text-[#B8E000] font-bold text-xl transition"
        >
          {SITE_CONFIG.email}
        </a>
      </div>
    </div>
  </Container>
</section>
